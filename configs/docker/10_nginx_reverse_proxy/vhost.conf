# multidomain certificate
# https://www.baeldung.com/linux/x-509-certificate-common-name-subject-alternative-names
# CN :
# - 100.124.238.99.nip.io
# SAN :
# - 100.124.238.99.nip.io
# - *.100.124.238.99.nip.io
# openssl command (https://stackoverflow.com/a/64344995) :
# openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout ./self.key -out ./self.crt -subj /C=FR/ST=Dev/L=Local/O=LocalDev/OU=Dev/CN=100.124.238.99.nip.io -addext "subjectAltName=DNS:100.124.238.99.nip.io,DNS:*.100.124.238.99.nip.io"

### WEB2MP3 ###

# redirect http:// to https://
server {
    server_name web2mp3.100.124.238.99.nip.io;
    listen 80;
    return 301 https://$host$request_uri;
}

server {
    server_name web2mp3.100.124.238.99.nip.io;

    # listen 80;

    http2 on;
    listen 443 ssl;
    # listen 5000 ssl;

    # http3 on;
    # listen 443 quic reuseport;

    ssl_certificate /etc/nginx/certs/self.crt;
    ssl_certificate_key /etc/nginx/certs/self.key;
    # ssl_protocols TLSv1.2 TLSv1.3;
    # quic_retry on;
    # ssl_early_data on;

    # root /var/www/public;
    # index index.php;

    # deny access to /admin from an IPv6 address (doesn't work with Filament)
    # actually, deny public access to /admin entirely
    # location ~ ^/admin(/.*)?$ {
    #     allow 192.168.0.0/24; # allow local subnet
    #     allow 100.64.0.0/10; # allow Tailscale VPN
    #     allow 172.16.0.1; # allow requests from the Docker host (i.e. the Docker internal subnet gateway)
    #     deny all; # deny everything else
    #     try_files $uri $uri/ /index.php?$args;
    # }

    # websocket server
    # location /app {
    location / {
        # proxy_http_version 1.1;
        proxy_set_header Host $http_host;
        proxy_set_header Scheme $scheme;
        proxy_set_header SERVER_PORT $server_port;
        proxy_set_header REMOTE_ADDR $remote_addr;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";

        # proxy_pass http://reverb:9001;
        # proxy_pass http://web2mp3_reverb:9001;
        proxy_pass http://web2mp3_nginx;
    }

    # location / {
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;

    #     proxy_pass http://web2mp3_nginx;
    # }

    # location / {
    #     allow all;
    #     # add_header Alt-Svc 'h3=":443"; ma=86400';
    #     try_files $uri $uri/ /index.php?$args;
    # }

    # location ~ \.php$ {
    #     include fastcgi_params;
    #     # fastcgi_pass app:9000;
    #     fastcgi_pass web2mp3_app:9000;
    #     fastcgi_index index.php;
    #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #     fastcgi_param PATH_INFO $fastcgi_path_info;
    #     fastcgi_buffers 16 16k;
    #     fastcgi_buffer_size 32k;
    # }
}
